import path from 'path';

export const mockMixinPath = path.resolve(
  __dirname,
  '../mocks/mockMixin.js',
);

export const mockComponentPath = path.resolve(
  __dirname,
  '../mocks/MockComponent.vue',
);
